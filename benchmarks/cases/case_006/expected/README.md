# Sample Project

A tiny demo project used for benchmark editing tasks.

## Installation

- Requires Python 3.11
- Install dependencies with `uv sync`
- Run the app with `uv run python main.py`

## Usage

Use this project to test precise markdown editing behavior.

## Notes

Keep edits minimal and focused.
